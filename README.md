# Youtube_Downloader (Repository)

This repo now contains a `webapp/` directory with a modern frontend and backend. The old scripts should be placed into `webapp/Legacy/` if you want to preserve them.

Follow the sub-READMEs to run the backend and frontend.
