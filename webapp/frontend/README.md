# Frontend (Vite + React)

This is a minimal React + Vite frontend. It provides a simple UI to fetch video info and download selected formats.

Quick start (PowerShell):

```powershell
# from webapp/frontend
npm install
npm run dev
```

Default expects backend at `http://localhost:8000`. Change calls in `src/App.jsx` if your backend runs elsewhere.
